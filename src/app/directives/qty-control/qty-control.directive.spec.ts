import { QtyControlDirective } from "./qty-control.directive";

describe("QtyControlDirective", () => {
  it("should create an instance", () => {
    const directive = new QtyControlDirective();
    expect(directive).toBeTruthy();
  });
});
